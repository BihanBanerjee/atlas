[project]
name = "atlas"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Bihan Banerjee", email = "banerjeebihan456@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.124.0",
    "openai>=3.3.1",
    "python-dotenv>=1.2.3",
    "qdrant-client>=1.19.0",
    "tiktoken>=0.14.0",
]

[project.scripts]
atlas = "atlas:main"

[build-system]
requires = ["uv_build>=0.8.13,<0.9.0"]
build-backend = "uv_build"
